import 'alerts_screen.dart';

/// The dashboard's notification bell opens the same Notification Center
/// used by the bottom nav tab, so behavior stays consistent everywhere.
class NotificationsScreen extends AlertsScreen {
  const NotificationsScreen({super.key});
}
